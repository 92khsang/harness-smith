"""Runtime adapters.

An adapter maps one agent runtime's own layout and loading rules onto this project's artifact
model. v1 ships exactly one, for Claude Code; adding a runtime means adding an adapter rather
than changing the artifact model.
"""
